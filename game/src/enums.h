#ifndef GAME_ENUMS_H
#define GAME_ENUMS_H

enum Directions {
    RIGHT=0,
    UP=1,
    LEFT=2,
    DOWN=3
};

enum TileType {
    EMPTY = 0,
    ROAD = 1,
    BASIC_TOWER = 2,
    CIRCLE_TOWER = 3
};

#endif //GAME_ENUMS_H
