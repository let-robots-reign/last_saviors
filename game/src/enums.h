#ifndef ENUMS_HPP
#define ENUMS_HPP

enum TileType {
    EMPTY = 0,
    ROAD = 1,
    BASIC_TOWER = 2,
    CIRCLE_TOWER = 3
};
enum Directions {
    RIGHT = 0,
    UP = 1,
    LEFT = 2,
    DOWN = 3
};

#endif
